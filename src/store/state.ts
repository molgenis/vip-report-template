import {State} from '@/types/State'

const state: State = {
    metadata: null,
    samples: null,
    selectedSample: null,
    selectedSamplePhenotypes: null,
    records: null
}

export default state